# Claude State — 2026-05-11

## 【Current State】

### zenpix ドキュメントサイト（website/）
- Dev server 起動確認済み（port 4322）
- i18n 設定完了・動作確認済み
  - `/` → English（root locale）
  - `/ja/` → Japanese
  - サイドバーの翻訳ラベルも設定済み
- テーブル表示バグ（th 背景が角丸を突き破る）→ 解決済み
  - `rehype-table-wrapper.mjs` で `div.table-scroll-wrapper` をラップ
  - `display: table` で Starlight の `@layer` による `display: block` を上書き
  - `border-collapse: separate; border-spacing: 0; overflow: hidden; border-radius: 12px` を `<table>` に適用
- 言語切り替えを Amulet と同じデザインに変更（作業完了・未確認）
  - `website/src/components/LanguageToggle.astro` を新規作成
  - Amulet の EN/JA ピル型トグルを cyan (#22d3ee) カラーに合わせて移植
  - `astro.config.mjs` に `components: { LanguageSelect: './src/components/LanguageToggle.astro' }` を追加
  - ブラウザでの目視確認はまだ

### リリース記事・SNS
- Ghost記事公開済み: https://tsukasa-art.com/blog/zenpix-release
- X投稿ドラフト完成済み: `docs/drafts/x-initial-release.md`（URL埋め込み済み）
- Zenn記事: 未着手

## 【Next Action】

### 直近（SNS・記事）
1. **X投稿**: `docs/drafts/x-initial-release.md` のドラフトをそのまま投稿
   - 画像2枚: zenpix-banner.jpg（1/3）、zenpix-logo.jpg（2/3）
2. **Zenn登録**: https://zenn.dev → GitHubアカウントでOAuth登録
3. **Zenn記事執筆**: タイトル候補 →「SharpのAVIFエンコードが低スペックVPSで詰まる問題をZig製ライブラリで解決した」
   - Ghost版（個人ストーリー寄り）と差別化し、技術解決・検索流入狙いの構成
   - 深度（使い方中心 or 実装判断含む）を確認してから書く

### ドキュメントサイト（再開時）
1. ブラウザで言語切り替えUIを確認（http://localhost:4322/）
2. 変更をコミット（website/ 以下、CLAUDE.md、README.md）
3. Cloudflare Pages へデプロイ（zenpix.tsukasa-art.com）

## 【Key Context】

### SNS戦略
- Zenn登録はGitHub OAuthのみ（メールアドレス不要）
- Noteはやらない（一般層向けで技術devがいない）
- XはSNSより検索流入（Zenn SEO）優先。Xは2フォロワーなので即効性なし、ハッシュタグ流入程度
- Ghost: staffDeviceVerification=falseにしてある（SMTP未設定のため）。ChromeでUIバグあり→Safari推奨

### LanguageToggle.astro（新規作成）のポイント
- Starlight の `LanguageSelect` スロットをオーバーライド
- URL パターン: `/ja/` プレフィックスで判定・切り替え
- アクティブ色: `#22d3ee`（cyan）、`text-shadow: 0 0 12px rgba(34, 211, 238, 0.5)`
- ボーダー: `rgba(34, 211, 238, 0.12)`、hover で `rgba(34, 211, 238, 0.35)`
- Amulet との差分: gold → cyan に変更。他はほぼ同じ構造

### astro.config.mjs の現在の i18n 設定

```js
defaultLocale: 'root',
locales: {
  root: { label: 'English', lang: 'en' },
  ja: { label: '日本語', lang: 'ja' },
},
components: {
  LanguageSelect: './src/components/LanguageToggle.astro',
},
sidebar: [
  { slug: 'index', label: 'Getting Started', translations: { ja: 'はじめに' } },
  { slug: 'cli', label: 'CLI Guide', translations: { ja: 'CLI ガイド' } },
  { slug: 'api', label: 'API Reference', translations: { ja: 'API リファレンス' } },
  { slug: 'benchmarks', label: 'Benchmarks', translations: { ja: 'ベンチマーク' } },
  { slug: 'environments', label: 'Environments & Troubleshooting', translations: { ja: '動作環境・トラブルシューティング' } },
],
```

### コンテンツファイル構成

```
src/content/docs/           ← English（root）
  index.md, cli.md, api.md, benchmarks.md, environments.md
src/content/docs/ja/        ← Japanese
  index.md, cli.md, api.md, benchmarks.md, environments.md
```

### テーブル CSS の核心（starlight-theme.css）
- `.table-scroll-wrapper` → `overflow-x: auto`
- `.sl-markdown-content table` → `display: table` が必須（Starlight の `@layer` が `display: block` を設定するため）
- `border-collapse: separate; border-spacing: 0; overflow: hidden; border-radius: 12px` を table に直接適用

### rehype-table-wrapper.mjs

```js
import { visit } from 'unist-util-visit';
import { h } from 'hastscript';

export function rehypeTableWrapper() {
  return (tree) => {
    visit(tree, 'element', (node, index, parent) => {
      if (node.tagName !== 'table' || !parent || index == null) return;
      const wrapper = h('div.table-scroll-wrapper', [node]);
      parent.children.splice(index, 1, wrapper);
      return index + 1;
    });
  };
}
```
