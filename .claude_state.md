# Claude State — zenpix

## 最終更新: 2026-05-25 13:09 JST

## 【Current State】

### Zig 0.16.0 移行（完了・コミット済み）
- ✅ `zig build` グリーン
- ✅ `zig build test` グリーン（c82006a）
- 修正箇所:
  - `build.zig`: linkSystemLibrary の pkg-config 自動呼び出し対応、ステップグラフ刷新
  - `src/main.zig`: テスト配列 `[_][]const u8` → `[_][:0]const u8`
  - `src/pipeline/decode.zig`, `src/root.zig`: `std.fs.cwd()` → `std.Io.Dir.cwd()`、`readFileAlloc` シグネチャ変更（`io` パラメータ追加、サイズ上限 `.limited(N)`）
  - `src/pipeline/resize.zig`, `src/platform.zig`, `src/platform/native.zig`: minor API 互換修正

### バージョン
- 最新: 0.9.0（npm publish 済み・GitHub Release 済み）
- VPS（tsukasa-art.com）: 0.9.0 更新済み

## 【Next Action】

1. CI (`build-native.yml`) を Zig 0.16.0 に更新
2. ドキュメントサイト（website/）の言語切り替えUI確認・Cloudflare Pages デプロイ（5/11未完のまま）

## 【Key Context】

### Zig 0.16 の主な API 変更
- `std.fs.cwd()` → `std.Io.Dir.cwd()`（テストコード内）
- `readFileAlloc(allocator, path, size)` → `readFileAlloc(io, path, allocator, .limited(size))`
- テスト内の文字列配列: `[_][]const u8` → `[_][:0]const u8`（センチネル要求）
- `build.zig` の `linkSystemLibrary` は 0.16 で pkg-config を自動呼び出すため、手動 `addSystemIncludePath` と重複しない

### npm publish 手順
- トークン: Dashlane「zigpix-publish」（Granular Access Token・Bypass 2FA有効）
- `docs/release.md` の手順に従う

### zenpix デプロイ（VPS）
- `npm list zenpix` でバージョン確認
- `bun install` で更新（`npx zenpix --version` は VPS 環境では動かない）
