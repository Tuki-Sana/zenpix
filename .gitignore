.zig-cache/
zig-out/
dist/

# Emscripten / cmake intermediate build artifacts
build/

# mise local override (グローバル設定があれば不要)
# .mise.toml

# Node.js
node_modules/
package-lock.json
js/dist/

# Pre-built native binaries for npm optional packages。
# CI artifacts をここへコピーしてから `npm publish` する。リポジトリにはコミットしない。
npm/*/libpict.dylib
npm/*/libpict.so
npm/*/libpict.dll

# Claude セッション状態（一時ファイル、コミット不要）
.claude_state.md

# メンテ用のローカルメモ（リポジトリにコミットしない）
docs/LOCAL.md
docs/TECH_MEMO.md

# ブログ・X・Note 投稿草稿（公開しない）
docs/drafts/

# Benchmark results (generated by CI/local runs, not committed)
bench/results/
bench/results-*/
bench-results-*/

# Test output images (generated by test-icon-processing.ts)
test/ffi/out-icon/

# Test fixtures (local assets, not committed)
test/fixtures/baitotime-icons/

# Editor（個人設定はコミットしない）
.vscode/

# Benchmark fixtures (fan art / derivative works — do not publish)
test/fixtures/bench_chara_*.png

# OS
.DS_Store

review/